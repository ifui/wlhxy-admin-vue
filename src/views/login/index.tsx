import { defineComponent } from 'vue'

export default defineComponent({
  name: 'Login',
  setup: () => {
    return () => {
      return (
        <>
          <div class="mt-2">Login</div>
        </>
      )
    }
  }
})
